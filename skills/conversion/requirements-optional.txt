# Optional dependency list (conversion skill) — all optional, install on demand; skip = take the degradation chain
# Version floors from external evaluation (2026-08-12, research/absorb-conversion-tools.md); reference only.
# Install example: pip install python-docx  /  pandoc — see each project's official docs
python-docx>=1.1.0      # chain ①: inline .docx generation (Word delivery)
mammoth>=1.6.0          # ingestion: .docx → Markdown
python-pptx>=0.6.21     # ingestion: .pptx → Markdown
latex2mathml>=3.77.0    # formulas: LaTeX → OMML (editable Word formulas; default keeps LaTeX source)
