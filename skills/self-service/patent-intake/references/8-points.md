# 8要点展开（推荐完整结构）

本文件为交底书推荐完整结构，推荐按8要点完整展开；未覆盖项在 `drafts/申请信息.md` 注明豁免理由，不编造。

> 八要点、图与式均不设上限，不硬凑。分型豁免：机械/结构类无公式可记 `N/A`；算法/控制/信号/优化/调度类⑤⑥⑦推荐具备——⑤必要源码/伪代码≥1、⑥物理变量表≥1、⑦推导≥1段且公式≥1式，缺项需在 `drafts/申请信息.md` 记 `formula: N/A — 无公式承载（理由）`，不编造。规范描述与公式并存：分步实施描述讲清数据流向与控制逻辑，公式给可计算定义，二者互指不替代；专业度由可复现深度体现，不在标题或正文中自称语体。

1. **总体** — 系统/方法总体架构与输入输出边界
2. **设计构思** — 核心发明构思与技术原理
3. **分步实施描述** — 按技术逻辑分步阐明输入/处理/输出与流向，3-8步，标题自描述如 `分步实施描述（6步）` 或 `技术方案分步（6步）`；不用 `S1→Sn / M1→Mn` 符号堆砌
4. **可自编程度** — 可复现程度与工程化落地说明
5. **必要源码摘录** — 关键算法/流程的必要源码或伪代码（非算法类 `N/A`；算法类推荐≥1段10-30行，标注输入/输出与关键行含义，分步实施描述中指回伪代码行号）
6. **物理变量释义** — 全部物理量、符号、量纲、取值范围与边界（无公式时 `N/A`；有式时每式紧跟变量表，同页可读）
7. **推导** — 核心公式的来源、推导与等价变形（无公式时 `N/A`；有式时写清来源是推导/实验拟合/设计选择，并给等效替代形式）
8. **硬件** — 硬件组成、部署形态与软硬件分工；末尾以实施例收束（纯算法复用通用硬件时 `N/A: 复用通用服务器`；实施例需给出参数取值+执行轨迹+输出结果，具可复现数值）

**图/式**：推荐≥1图+≥1式+≥1数值实施例，上不封顶；豁免按分型在 `application-info.md` 记 `figure-availability: N/A` / `formula: N/A — 无公式承载（理由）`。已呈现图含 `图N …` 题注且成对 `figures/embed/*.png` + `figures/source/*`；已呈现公式每式紧跟变量表（含义/单位/输入输出/取值范围）与边界，同页可读。
